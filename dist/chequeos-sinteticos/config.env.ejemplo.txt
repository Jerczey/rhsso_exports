# Añadir a ejecucion/config.env (ajustar valores del cliente)

KC_SERVER=https://sso.ejemplo.local/auth
KC_REALM=mi-realm

# --- PostgreSQL (synthetic-check-pg.sh) ---
PG_HOST=db.ejemplo.local
PG_PORT=5432
PG_USER=rhsso_read
PG_PASSWORD=changeme
PG_DB=rhsso

# Opcional lab Podman: si no hay psql en el host, el script usa podman exec
# PG_CONTAINER=my-postgres

# --- Microsoft SQL Server (synthetic-check-mssql.sh) ---
# DB_TYPE=mssql
# MSSQL_HOST=sqlserver.ejemplo.local
# MSSQL_PORT=1433
# MSSQL_DB=rhsso
# MSSQL_USER=rhsso_read
# MSSQL_PASSWORD=changeme
# MSSQL_TRUST_CERT=true

# --- Balanceador (opcional) ---
# LB_URL=https://sso.ejemplo.local/auth

# --- Login sintético (opcional) ---
# SYNTH_REALM=mi-realm
# SYNTH_CLIENT_ID=mi-app
# SYNTH_USER=usuario.prueba
# SYNTH_PASSWORD=changeme
