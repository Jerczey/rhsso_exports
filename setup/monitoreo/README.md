# Monitoreo — laboratorio interno

Uso interno (`setup/`). **No entregar al cliente.**

| Recurso | Descripción |
|---------|-------------|
| [CHEQUEOS-SINTETICOS-PG-LAB.md](CHEQUEOS-SINTETICOS-PG-LAB.md) | Documentación (PostgreSQL Podman + HAProxy + SPI) |
| `./synthetic-check-pg-lab.sh` | Script automático del lab |
| `./synthetic-check.sh` | Alias del script anterior |

Para el cliente, ver `ejecucion/monitoreo/` (PostgreSQL nativo y SQL Server, sin Podman).
